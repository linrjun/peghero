# 弹珠勇者 v1.0 重构版

## 当前边界

- `index.html`：静态入口，只负责加载样式和模块。
- `styles.css`：完整移动端界面样式。
- `src/main.js`：启动入口与运行时加载。
- `src/config.js`：版本与存档键。
- `src/data/`：后续迁移弹珠、钉子、遗物、敌人、事件与地图配置。
- `src/systems/`：后续迁移战斗、地图、商店、铁匠、存档和流派规则。
- `src/ui/`：后续迁移 HUD、弹窗和地图渲染。

本次重构优先保证旧版本功能完整与可部署，避免一次性拆分导致大量回归问题。
后续每个版本将逐步迁出稳定模块，并持续保持可试玩状态。
